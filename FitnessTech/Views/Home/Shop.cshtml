@model IEnumerable<FitnessTech.Data.Entities.Product>

@{
    ViewData["Title"] = "Shop";
}

<h2>Shop</h2>


@*<table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Size)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SupplementDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SupplementId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producer)
            </th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Size)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SupplementDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SupplementId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Producer)
                </td>

            </tr>
        }
        </tbody>
    </table>*@

<p>We have @Model.Count() products in stock</p>
<hr />
<div class="container">
@foreach (var m in Model)
{
    <div class="col-md-3">
        <div class="well well-sm">
            <img src="~/images/@(m.SupplementId).png" class="img-responsive" alt="@m.Title" />
            <h3 style="color: dodgerblue">@m.Title -  @m.Category</h3>
            <ul class="custom-bullet">
                <li>@m.SupplementDescription</li>
                <li>By: @m.Producer</li>
                <li>Net wt: @m.Size</li>
            </ul>
            <div class="row">
                <div class="col-xs-6">
                    <button id="buyButton" class="btn btn-info" >Buy</button>
                </div>
                <div class="col-xs-6">
                     <p style="color: dodgerblue" class="bottom-align-text">Price: @m.Price</p>

                </div>
 
            </div>
        </div>
    </div>

}
</div>
